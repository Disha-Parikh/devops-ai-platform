apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: devops-ai-platform
  namespace: argocd

spec:
  project: default

  source:
    repoURL: https://github.com/YOUR_GITHUB/devops-ai-platform
    targetRevision: HEAD
    path: gitops

  destination:
    server: https://kubernetes.default.svc
    namespace: default

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
